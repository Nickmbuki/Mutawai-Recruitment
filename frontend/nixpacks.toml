[phases.setup]
nixPkgs = ["nodejs_22", "pnpm"]

[phases.install]
cmds = ["pnpm install --frozen-lockfile"]

[phases.build]
cmds = ["pnpm --filter frontend run build"]

[start]
cmd = "pnpm --filter frontend run preview -- --host 0.0.0.0 --port $PORT"
